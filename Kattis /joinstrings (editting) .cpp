#include<iostream>
using namespace std;

const int N = 169420;
const int HEAD = 0, END = -1;

string s[N];
int list[N];

void init() {
	
}

void process() {
	int a, b;
	while (cin >> a >> b) {
		
	}
	
} 3-2-4-1

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int n; cin >> n;
	
	for (int i=1; i<=n; i++) {
		cin >> s[i];
	}
	
	init();
	process();
	
	return 0;
}
